import React from 'react';
import styled from 'styled-components';

const UserProfile = () => {
  return <SUserProfile>UserProfile</SUserProfile>;
};

export default UserProfile;

const SUserProfile = styled.section``;
